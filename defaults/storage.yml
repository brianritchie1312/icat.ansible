---

storage_src: http://{{ sub_domain }}.icatproject.org/{{ url_mvn }}repo/org/icatproject/ids.storage_file/{{ storage_version }}/{{ storage_zip }}
storage_zip: ids.storage_file-{{ storage_version }}-distro.zip
storage_path: "{{ user_home }}/ids.storage_file"

dir_main: "{{ user_home }}/{{ icat_name }}/main"
dir_archive: "{{ user_home }}/{{ icat_name }}/archive"

storage_main: ids.storage_file.main.properties
storage_archive: ids.storage_file.archive.properties
storage_setup: ids.storage_file-setup.properties

