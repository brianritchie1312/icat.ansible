---

lucene_version: 1.1.0

lucene_src: "https://repo.icatproject.org/repo/org/icatproject/icat.lucene/{{ lucene_version }}/{{ lucene_zip }}"
lucene_zip: "icat.lucene-{{ lucene_version }}-distro.zip"
lucene_path: "{{ user_home }}/icat.lucene"

lucene_setup: setup.properties
lucene_prop: run.properties

lucene_dir: "{{ user_home }}/icat/data/lucene"
