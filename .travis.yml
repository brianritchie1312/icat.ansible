#---Notifcations---

notifications:
  email: false

#---Language---

language: python

#---Branches---

branches:
  # Whitelist
  only:
    - master

#---Environment---

sudo: required

#---Services---

#-----BEGIN BUILD-----

 #--Before Install--

 #--Install--

install:

  - pip install ansible

 #--Before Script--

 #--Script--

script:
  - "echo 'allow_world_readable_tmpfiles=True' > ansible.cfg"
  - "ANSIBLE_CONFIG=ansible.cfg ansible-playbook -i tests/inventory tests/test.yml --connection=local -vvvv --sudo --extra-vars "authn_simple=true authn_db=true authn_ldap=true authn_anon=true"
  
 #--After Script--

 #--Before Deploy--

 #--Deploy--

 #--After Deploy--
